# Node-RED + palettes nécessaires au flow (dashboard, MongoDB, InfluxDB).
FROM nodered/node-red:4.0

RUN npm install --no-audit --no-fund \
      node-red-dashboard \
      node-red-node-mongodb \
      node-red-contrib-influxdb
